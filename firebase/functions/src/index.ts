import { firebase } from '../../../src-api';

export const aiText = firebase.aiText;
export const aiImage = firebase.aiImage;
export const chatDelete = firebase.chatDelete;
export const chatShare = firebase.chatShare;